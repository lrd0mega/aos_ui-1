{
  "version": 2,
  "builds": [
    { "src": "public/*", "use": "@vercel/static-build" },
    { "src": "src/*", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}